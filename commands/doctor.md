---
description: Diagnose the local Rhize Tasks service safely
argument-hint: "[component to inspect]"
allowed-tools: [Bash, Read]
---

Use `$rhize-tasks-doctor` for this request. Pass `$ARGUMENTS` as a diagnostic focus and keep the workflow read-only unless the user separately approves a bounded repair. Never ask for secrets in chat.
